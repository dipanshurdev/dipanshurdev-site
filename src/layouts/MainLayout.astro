---
import { Footer } from "../components";
import Navbar from "../components/Navbar/Navbar";
import Layout from "./Layout.astro";
import "@fontsource-variable/dosis";

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<Layout title={title}>
  <main
    class="flex items-center justify-between flex-col mx-auto p-4 w-full h-full min-h-screen overflow-y-scroll overflow-x-hidden max-w-[70rem] sm:max-w-screen-lg text-1.5rem leading-1.6"
  >
    <Navbar client:load />
    <slot />
    <Footer />
  </main>
</Layout>

<style>
  main {
    /* display: flex;
    align-items: center;
    justify-content: space-between;
    flex-direction: column;
    margin: auto; */
    /* padding: 1rem;
    width: 100%;
    height: 100%;
    min-height: 100vh;
    overflow-y: scroll;
    overflow-x: hidden;
    max-width: 70rem; */
    /* font-family: "Dosis Variable"; */
    /* font-family: "Open Sans"; */
    font-family: "Dosis Variable";
    /* font-size: 1.5rem;
    line-height: 1.6;
    background: transparent; */
  }
</style>
